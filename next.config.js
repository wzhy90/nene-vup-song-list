const nextConfig = {
  output: 'export',
  trailingSlash: true,

  images: {
    loader: "custom",
    unoptimized: true, 
  },

  // basePath: '/nene-vup-song-list',
  // assetPrefix: '/nene-vup-song-list/',
};

module.exports = nextConfig;
